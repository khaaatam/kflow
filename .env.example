# --- BOT IDENTITY ---
BOT_NAME=JikaeLBot
BOT_CREATORS=JikaeL
# Format: number1,number2
BOT_OWNER_NUMBERS=6289608506367,62881081132332
# Format: whatsappId1=Name1,whatsappId2=Name2
# NOTE: pakai '=' (bukan ':') supaya ID yang mengandung ':' tetap kebaca.
BOT_USERS=6289608506367@c.us=Tami,6283806618448@c.us=Dini

# --- SERVER ---
PORT=3000
LOG_NUMBER=62881081132332@c.us

# --- DATABASE ---
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=kflow_db
DB_CONNECTION_LIMIT=10

# --- AI ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# --- PUPPETEER ---
# Opsional. Kalau dikosongkan, app akan auto-detect dari path umum (termasuk Termux).
# Isi ini kalau auto-detect gagal.
PUPPETEER_EXECUTABLE_PATH=
PUPPETEER_HEADLESS=true
# Format: --arg1,--arg2
PUPPETEER_ARGS=--no-sandbox,--disable-setuid-sandbox,--disable-dev-shm-usage,--disable-gpu,--no-first-run,--no-zygote
